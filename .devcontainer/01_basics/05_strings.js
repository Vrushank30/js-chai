const name = "vrushank";
const repoCount = 1;
// console.log(name+"'s repocount : "+repoCount); old way
// new and preferred way ->
console.log(`Hello my name is ${name} and my repocount is ${repoCount}`);
